---
import { Image } from 'astro:assets';
import { site } from '@data/site';
import ContactBar from './ContactBar.astro';

interface Props {
  variant: 'sidebar' | 'mobile';
}

const { variant } = Astro.props;
const isSidebar = variant === 'sidebar';
---

{isSidebar ? (
  <>
    <div class="relative group mb-8 reveal-content flex justify-center" style="animation-delay: 100ms;">
      <div class="balloon-float w-36 h-36
       md:w-45 md:h-45 rounded-full border-2 border-accent overflow-hidden shadow-[0_0_50px_color-mix(in_srgb,var(--color-accent)_33%,transparent)]">
        <Image
          src={site.hero.avatarSrc}
          alt={site.hero.avatarAlt}
          class="h-full w-full scale-110 object-cover"
          loading="eager"
          width={180}
          height={180}
        />
      </div>
      <div class="balloon-float absolute -z-10 top-0 left-0 h-full w-full rounded-full"></div>
    </div>

    <div class="flex flex-col items-center text-center">
      <a href={import.meta.env.BASE_URL} class="text-3xl font-bold tracking-tight text-white transition">
        {site.hero.name}
      </a>
      <p class="mt-3 text-xl font-medium text-slate-light">{site.hero.role}</p>
      <p class="body-copy text-xs mt-2 max-w-xs">{site.hero.tagline}</p>
      <p class="body-copy mt-2 max-w-xs">{site.hero.contact}</p>

      <div class="mt-2 flex justify-center gap-4">
        <ContactBar showResume />
      </div>
    </div>
  </>
) : (
  <div class="flex flex-col items-center text-center">
    <div class="relative mb-4">
      <div class="h-32 w-32 rounded-full border-2 border-accent overflow-hidden shadow-[0_0_40px_color-mix(in_srgb,var(--color-accent)_28%,transparent)]">
        <Image
          src={site.hero.avatarSrc}
          alt={site.hero.avatarAlt}
          class="h-full w-full object-cover"
          loading="eager"
          width={128}
          height={128}
        />
      </div>
    </div>
    <h1 class="text-4xl font-bold tracking-tight text-white transition">
      {site.hero.name}
    </h1>
    <p class="mt-3 text-xl font-semibold tracking-tight text-slate-light">{site.hero.role}</p>
    <div class="mt-4 max-w-prose space-y-3 text-lg leading-relaxed text-slate">
      <p>{site.hero.tagline}</p>
      <p>{site.hero.contact}</p>
    </div>
    <div class="mt-2 mb-6 flex items-center justify-center gap-4">
      <ContactBar showResume />
    </div>
  </div>
)}
